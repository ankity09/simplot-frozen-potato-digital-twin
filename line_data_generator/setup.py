# Example setup.py for your package
from setuptools import setup, find_packages

setup(
    name="line_data_generator",
    version="0.1.0",
    packages=["line_data_generator"],
    package_dir={"line_data_generator": "."},
    install_requires=[],
)